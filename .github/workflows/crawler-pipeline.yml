name: Run Crawler Pipeline

on:
  workflow_dispatch:  # Allows you to manually trigger the workflow if needed

# This workflow has no `schedule:` trigger of its own - it's fired externally
# every 15 minutes by the cron service behind CRAWLER_CRONJOB_HEARTBEAT below,
# regardless of whether the previous run finished. Without this, a slow run
# (confirmed: Places Leisure 429s pushed a single run past 30+ minutes) stacks
# up multiple concurrent runs fighting over the same DB tables. Cancelling the
# stale run on each new trigger is safe: all writes go through
# insert_records_to_table's idempotent upsert, so a cancelled mid-run pipeline
# just leaves a partial-but-valid refresh, not corrupted data.
concurrency:
  group: crawler-pipeline
  cancel-in-progress: true

jobs:
  badminton-crawler-pipeline:
    runs-on: ubuntu-latest
    name: "Badminton Crawler"
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker and log in to GitHub Container Registry
        run: |
          echo ${{ secrets.GHCR }} | docker login ghcr.io -u yasir-khalid --password-stdin

      - name: Write Firebase JSON secret to file
        run: |
          echo "${{ secrets.FIREBASE_ADMIN_SDK_JSON }}" > /tmp/sportscanner-firebase-adminsdk.json

      - name: Create .env file from secret
        run: |
          echo "${{ secrets.ENV_FILE_CONTENT }}" > .env  # Store the content of your .env file in the secret "ENV_FILE_CONTENT"

      - name: Run crawler pipeline container
        run: |
          echo "Running container for image (tag: latest) to run data crawlers pipeline"
          docker run --rm --platform=linux/amd64 --network=host --env-file .env \
            -v /tmp/sportscanner-firebase-adminsdk.json:/app/sportscanner-firebase-adminsdk.json \
            ghcr.io/sportscanner/app-crawlers:latest \
            python sportscanner/crawlers/pipeline.py --task badminton
  
  squash-crawler-pipeline:
    needs: badminton-crawler-pipeline
    runs-on: ubuntu-latest
    name: "Squash Crawler"
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker and log in to GitHub Container Registry
        run: |
          echo ${{ secrets.GHCR }} | docker login ghcr.io -u yasir-khalid --password-stdin

      - name: Write Firebase JSON secret to file
        run: |
          echo "${{ secrets.FIREBASE_ADMIN_SDK_JSON }}" > /tmp/sportscanner-firebase-adminsdk.json

      - name: Create .env file from secret
        run: |
          echo "${{ secrets.ENV_FILE_CONTENT }}" > .env  # Store the content of your .env file in the secret "ENV_FILE_CONTENT"

      - name: Run crawler pipeline container
        run: |
          echo "Running container for image (tag: latest) to run data crawlers pipeline"
          docker run --rm --platform=linux/amd64 --network=host --env-file .env \
            -v /tmp/sportscanner-firebase-adminsdk.json:/app/sportscanner-firebase-adminsdk.json \
            ghcr.io/sportscanner/app-crawlers:latest \
            python sportscanner/crawlers/pipeline.py --task squash

  pickleball-crawler-pipeline:
    needs: squash-crawler-pipeline
    runs-on: ubuntu-latest
    name: "Pickleball Crawler"
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker and log in to GitHub Container Registry
        run: |
          echo ${{ secrets.GHCR }} | docker login ghcr.io -u yasir-khalid --password-stdin

      - name: Write Firebase JSON secret to file
        run: |
          echo "${{ secrets.FIREBASE_ADMIN_SDK_JSON }}" > /tmp/sportscanner-firebase-adminsdk.json

      - name: Create .env file from secret
        run: |
          echo "${{ secrets.ENV_FILE_CONTENT }}" > .env  # Store the content of your .env file in the secret "ENV_FILE_CONTENT"

      - name: Run crawler pipeline container
        run: |
          echo "Running container for image (tag: latest) to run data crawlers pipeline"
          docker run --rm --platform=linux/amd64 --network=host --env-file .env \
            -v /tmp/sportscanner-firebase-adminsdk.json:/app/sportscanner-firebase-adminsdk.json \
            ghcr.io/sportscanner/app-crawlers:latest \
            python sportscanner/crawlers/pipeline.py --task pickleball

  padel-crawler-pipeline:
    needs: pickleball-crawler-pipeline
    runs-on: ubuntu-latest
    name: "Padel Crawler"
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker and log in to GitHub Container Registry
        run: |
          echo ${{ secrets.GHCR }} | docker login ghcr.io -u yasir-khalid --password-stdin

      - name: Write Firebase JSON secret to file
        run: |
          echo "${{ secrets.FIREBASE_ADMIN_SDK_JSON }}" > /tmp/sportscanner-firebase-adminsdk.json

      - name: Create .env file from secret
        run: |
          echo "${{ secrets.ENV_FILE_CONTENT }}" > .env  # Store the content of your .env file in the secret "ENV_FILE_CONTENT"

      - name: Run crawler pipeline container
        run: |
          echo "Running container for image (tag: latest) to run data crawlers pipeline"
          docker run --rm --platform=linux/amd64 --network=host --env-file .env \
            -v /tmp/sportscanner-firebase-adminsdk.json:/app/sportscanner-firebase-adminsdk.json \
            ghcr.io/sportscanner/app-crawlers:latest \
            python sportscanner/crawlers/pipeline.py --task padel

  tennis-crawler-pipeline:
    needs: padel-crawler-pipeline
    runs-on: ubuntu-latest
    name: "Tennis Crawler"
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker and log in to GitHub Container Registry
        run: |
          echo ${{ secrets.GHCR }} | docker login ghcr.io -u yasir-khalid --password-stdin

      - name: Write Firebase JSON secret to file
        run: |
          echo "${{ secrets.FIREBASE_ADMIN_SDK_JSON }}" > /tmp/sportscanner-firebase-adminsdk.json

      - name: Create .env file from secret
        run: |
          echo "${{ secrets.ENV_FILE_CONTENT }}" > .env  # Store the content of your .env file in the secret "ENV_FILE_CONTENT"

      - name: Start FlareSolverr sidecar
        # Last-resort WAF bypass for ClubSpark venues that 403 on every date/run
        # even after TLS-impersonation retries (see anonymize/flaresolverr.py) -
        # confirmed live that these are solvable Cloudflare JS challenges, not a
        # network-level IP block, so a real headless browser can rescue them.
        # --network=host puts it on the same network namespace as the crawler
        # container below, so `localhost:8191` reaches it directly.
        run: |
          docker run -d --name flaresolverr --network=host \
            -e LOG_LEVEL=info \
            ghcr.io/flaresolverr/flaresolverr:latest
          for i in $(seq 1 15); do
            code=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8191/ || true)
            [ "$code" = "200" ] && echo "FlareSolverr ready" && exit 0
            sleep 2
          done
          echo "FlareSolverr did not become ready in time - continuing without it"

      - name: Run crawler pipeline container
        run: |
          echo "Running container for image (tag: latest) to run data crawlers pipeline"
          docker run --rm --platform=linux/amd64 --network=host --env-file .env \
            -e FLARESOLVERR_URL=http://localhost:8191/v1 \
            -v /tmp/sportscanner-firebase-adminsdk.json:/app/sportscanner-firebase-adminsdk.json \
            ghcr.io/sportscanner/app-crawlers:latest \
            python sportscanner/crawlers/pipeline.py --task tennis

      - name: Stop FlareSolverr sidecar
        if: always()
        run: docker rm -f flaresolverr || true

  update-hearbeart-monitor:
    needs:
      - badminton-crawler-pipeline
      - squash-crawler-pipeline
      - pickleball-crawler-pipeline
      - padel-crawler-pipeline
      - tennis-crawler-pipeline
    runs-on: ubuntu-latest
    steps:
      - name: Update heartbeat monitor
        run: |
          curl ${{ secrets.CRAWLER_CRONJOB_HEARTBEAT }}
